version: "3.8"

services:
  dev-postgres:
    image: postgres:17
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: porota
      POSTGRES_PASSWORD: porota
      POSTGRES_DB: porota
    restart: always
    networks:
      - lesson

networks:
  lesson:
